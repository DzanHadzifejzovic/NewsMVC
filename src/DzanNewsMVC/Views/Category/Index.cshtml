@using DzanNewsMVC.Models.Categories
@model IEnumerable<CategoryViewModel>


<p class="button"><a class="btn btn-primary" asp-controller="Category" asp-action="Insert">Unesi novi</a></p>

@if (Model != null)
{
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
                <th>Details</th>
            
            </tr>
        </thead>
        <tbody>
            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.Id</td>
                    <td>@r.Name</td>
                    <td>@r.Description</td>
                    <td><a asp-controller="Category" asp-action="Detail" asp-route-id="@r.Id">Detail</a></td>
                </tr>
            }
        </tbody>
    </table>
}